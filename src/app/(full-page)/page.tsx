import { Box, Button } from '@mui/material';

const HomePage = () => {
    return (
        <Box sx={{ height: 1 }}>
            <Button variant="contained" href="/login">
                Link
            </Button>
        </Box>
    );
};

export default HomePage;
